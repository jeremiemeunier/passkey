# Relying Party Name (displayed to users during authentication)
RP_NAME="Passkey Demo"

# Relying Party ID (usually your domain, e.g., "example.com" or "localhost")
RP_ID="localhost"

# Origin URL (the full URL where your app is hosted)
ORIGIN="http://localhost:3000"
